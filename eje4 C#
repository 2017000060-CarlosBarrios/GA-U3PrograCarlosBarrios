using System;

class Program
{
    static void Main()
    {
        Console.Write("Ingrese una frase: ");
        string frase = Console.ReadLine();

        char[] arreglo = frase.ToCharArray();
        Array.Reverse(arreglo);
        string fraseInvertida = new string(arreglo);

        Console.WriteLine($"Frase invertida: {fraseInvertida}");
    }
}
